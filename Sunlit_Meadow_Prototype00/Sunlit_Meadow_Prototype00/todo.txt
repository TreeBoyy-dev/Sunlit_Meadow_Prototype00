1. fix remeshing bug:
	- sometimes the adjacend chunk returns null even at the final remeshing

1. viewing cone:
	- don't render the chunks the player is not looking at

1. greedy meshing:
	- idk if its called that but what ever
	- make face/quad class instead of adding the face directly to the mesh
	- when making the chunkmesh split it into stages:
		-> get all faces
		-> simplefy faces (combine adjacend faces on the same level)
		-> load the simplefied faces into the mesh

1. add skybox
1. add more blocks
1. add stairs, slabs (check), walls etc (block varients in generall)