move rendering to seperate Render Thread

completely restructure chunk class
	- blocks stored in maps/sets/vectors not array
	- blockid are remapped to fit in smaller dataelements to safe on storage
	- last x bits of the id describe the blockstate
	- think about changing a chunk to be 16x16x256 instead of 16x16x16
		- basicly cchunks still ontop onanother but ony one per worldLayer
		- possibly a worldLayer class

(unfinished) change how the generated chunks are handled
	- new system:
		-> list rendered chunks
		-> list loaded chunks
		-> list rendered chunkmeshes
		-> when player moves the chunks to far away get removed and the closer ones added

(unfinished) fix remeshing bug:
	- sometimes the adjacend chunk returns null even at the final remeshing
	- or something else idk
	- only one of the six sides fails (alway the same side)(the bigger the renderdistance the more likely it is to happen)

(unfinished) viewing cone:
	- sometimes a chunk still disappers to early

greedy meshing:
	- idk if its called that but what ever
	- make face/quad class instead of adding the face directly to the mesh
	- when making the chunkmesh split it into stages:
		-> get all faces
		-> simplefy faces (combine adjacend faces on the same level)
		-> load the simplefied faces into the mesh

(mostly finished) add entity
	- data
	- physics
-> data needs to be adjusted when i need to idk what could be missing or something

block colission

UI and menues
	- add class to load/handle all textures
	- settings
	- textures/design/layout in generall
		-> !! get textures for hotbar

add more blocks
add stairs, slabs (check), walls etc (block varients in generall)

figure out a way to store block/item/entity data
	- then store the world in a file